---
type Props = {
	isTag?: boolean
}

const { isTag } = Astro.props
---

<h1
	class=`text-3xl md:text-5xl font-bold ${isTag ? '' : 'first-letter:uppercase'} md:leading-[1.3] max-w-[69vw] truncate`
>
	<slot />
</h1>
