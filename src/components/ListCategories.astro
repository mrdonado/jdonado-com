---
import { getCategories } from '@/utils'
import Category from '@/components/Category'

const categories = (await getCategories()).filter((category) => category !== 'Labs')

const { activeCategory } = Astro.props
---

<div
	class='relative flex flex-wrap min-w-full gap-[10px] md:gap-5 mb-6 justify-center md:justify-start'
>
	<Category />
	{
		categories.map((category: string) => (
			<Category name={category} activeCategory={activeCategory} />
		))
	}
	<Category name='Tags' />
	<Category name='Archive' />
</div>
